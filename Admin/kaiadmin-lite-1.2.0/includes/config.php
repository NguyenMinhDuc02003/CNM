<?php
/**
 * Admin module configuration.
 *
 * Set ADMIN_AUTH_ENABLED to false to bypass login checks (useful while building UI).
 * Turn it back to true when you are ready to enforce permissions.
 */
const ADMIN_AUTH_ENABLED = true;
